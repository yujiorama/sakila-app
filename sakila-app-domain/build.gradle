plugins {
    id 'java-library'
}

dependencies {
    api('org.springframework.boot:spring-boot-starter-data-jpa')
    api('org.postgresql:postgresql')
    implementation('org.flywaydb:flyway-core')
}

// disable spring-boot app configuration
tasks.jar.enabled = true
